/* eslint-disable react/prop-types */
import Modal from "./Modal";

const AddTaskModal = ({ isOpen, setIsOpen }) => {
  return (
    <Modal isOpen={isOpen} setIsOpen={setIsOpen} title={"Add task Modal"}>
      
    </Modal>
  );
};

export default AddTaskModal;
